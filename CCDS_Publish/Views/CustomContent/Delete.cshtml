@model qms.ViewModels.VMCustomContent


@Scripts.Render("~/bundles/jquery")
@Scripts.Render("~/bundles/jqueryval")
@{
    ViewBag.Title = "Custom Content Delete";
}

<div class="container">
    <div class="row">
        <h4 class="page-head-line">Custom Content > Delete</h4>
        <h3>Are you sure you want to delete this?</h3>
        <div>
            
            <dl class="dl-horizontal">
                @*<dt>
                    @Html.DisplayName("National?")
                </dt>*@

                @*<dd>
                    @if (Model.is_global > 0)
                    {
                        @Html.DisplayName("Yes")
                    }
                    else
                    {
                        @Html.DisplayName("No")
                    }
                </dd>*@

                <dt>
                    @Html.DisplayName("Content")
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.content)
                </dd>

                <dt>
                    @Html.DisplayName("URL")
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.is_url)
                </dd>

            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">
                    <input type="submit" value="Delete" class="btn btn-color" /> |
                    <button class="btn btn-color">
                        @Html.ActionLink("Back to List", "Index")
                    </button>
                </div>
            }
        </div>
        <br />
    </div>
</div>