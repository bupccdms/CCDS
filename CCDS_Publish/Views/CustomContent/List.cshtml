@model IEnumerable<qms.ViewModels.VMCustomContent>
<div class="row">
    <table class="table table-hover table-bordered" id="data">
        <tr id="Hedding">
            <th></th>
            <th>
                @Html.DisplayName("Content")
            </th>
            <th>
                @Html.DisplayName("URL?")
            </th>
        </tr>

        @foreach (var item in Model)
        {
        <tr>
            <td>
                @*@if (item.is_url > 0)
                {
                    <input type="radio" style="cursor:pointer" name="radio-content" id="@item.content" value="@item.content" />
                }
                else
                {
                    <input type="radio" style="cursor:pointer" name="radio-content" id="@item.custom_content_id" value="@item.custom_content_id" />
                }*@
                <input type="radio" style="cursor:pointer" name="radio-content" id="@item.custom_content_id" value="@item.custom_content_id" />
            </td>
            <td style="text-align:left">
                @Html.Raw(item.content)
            </td>
            <td>
                @if (item.is_url > 0)
                {
                    @Html.DisplayName("Yes")
                }
                else
                {
                    @Html.DisplayName("No")
                }
                @*@Html.DisplayFor(modelItem => item.is_url)*@
            </td>
        </tr>
        }

    </table>
</div>


















    @*<div class="row">       

        <div class="col-md-12 scrollbar table-scroll">

            <table class="table table-hover table-bordered" id="data">
                <tr id="Hedding">
                    <th>
                        @Html.DisplayName("Content")
                    </th>
                    <th>
                        @Html.DisplayName("URL?")
                    </th>
                </tr>

                @foreach (var item in Model)
                {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.content)
                    </td>
                    <td>
                        @if (item.is_url > 0)
                        {
                            @Html.DisplayName("Yes")
                        }
                        else
                        {
                            @Html.DisplayName("No")
                        }
                        @Html.DisplayFor(modelItem => item.is_url)*
                    </td>
                </tr>
                }

            </table>
        </div>
    </div>*@
    

